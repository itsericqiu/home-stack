# DevTools TLS Testing (Phase B)

- Use Chrome DevTools Network panel to inspect TLS handshakes for https://127.0.0.1:31443/
- In Security tab, inspect certificate chain and trust
- In Network panel, inspect TLS details per request (TLS version, cipher, handshake timings)
- Optional: include a small note about using self-signed certs and adding exceptions in the browser
