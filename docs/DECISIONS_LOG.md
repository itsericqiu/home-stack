# Decisions Log

Date | Decision | Context | Alternatives | Rationale | Owners | Status | Follow-ups
---|---|---|---|---|---|---|---
2026-09-10 | `install:` block as the migration/upgrade manifest; pins live in the owner's profile; upgrades are reviewed per-method procedures with backup, health check, auto-revert | Four operator-installed components — Pocket ID, tinyauth, Caddy, Hermes — had no upgrade procedure beyond a RUNBOOK recipe run by hand, with no drift visibility and no reviewed rollback; OpenCode/OpenChamber's self-updaters had the same gap. `docs/SERVICE_INTERFACE.md`'s "Planned fields" table already sketched `install:` (P5) | A generic `hs upgrade all` that walks every method uniformly (masks the real differences — Caddy needs sudo, Hermes needs a human decision, npm has no rollback); shell out to each upstream tool's own updater with no home-stack-level drift visibility or backup; store the pin in the registry itself rather than the profile (would force a public commit for every version bump, and conflates portable schema with deployment state) | Each `install.method` gets its own hand-written, reviewed procedure in `scripts/lib/upgrade.sh` rather than one generic algorithm, because the methods are not actually uniform: `github-release`/`source-go`/`npm-global`/`opencode` restart themselves end to end; `xcaddy` stops short of restarting the system LaunchDaemon (needs sudo) rather than leaving the daemon on an unverified binary; `hermes-pinned` requires `--yes` and a full commit SHA (never a tag) because it mutates `~/.hermes`, matching the RUNBOOK's existing "commit-pinned only" rule; `brew` is status-only. Every automated method backs up the previous binary to `.prev`, restarts, waits on the registry's own health signal, and auto-reverts on failure — the same backup-then-verify shape the manual RUNBOOK recipes already used, just made consistent and scriptable. `pin` is optional and lives in the owner's profile (deployment state), matching how every other identity value in this repo is split from portable schema; `profiles/default/services.yaml`'s examples show realistic values with no live pin | OpenTeam | Implemented | `hs upgrade all` remains out of scope (ROADMAP already excluded it); D.2-style federation of the upgrade log into Admin's doctor surface is a candidate follow-up, not yet built
2026-09-10 | Public release Phase B: deployment record moves to a private overlay; docs are scrubbed and split accordingly | `docs/PUBLIC_RELEASE.md` §5 specified the docs scrub following Phase A's profile-resolver work; the audit (§3) found deployment facts (Hermes version pins, commits, verification transcripts, deployed provider/model routing, Keychain item names) and personal-project/provider references leaking into public-facing docs and planning docs | Keep the deployment record inline and just redact secrets (still identifies the machine/owner); publish the overlay content publicly too (defeats the point); delete the facts outright instead of preserving them for the owner | RUNBOOK/STATUS/VERIFICATION/CHANGELOG now state capabilities and procedures only; every removed fact was preserved verbatim in the owner's private overlay (`INSTALLED.md`, `hermes-deployed.md`, `migration-private.md`) rather than dropped. The placeholder pass, README rewrite, attic removal, LICENSE/SECURITY.md/CONTRIBUTING.md additions, and the tier-table governance rule complete the rest of §5's B1–B7. `tests/public-hygiene.test.sh` now scans the whole repository including docs | OpenTeam | Implemented | Phase C (publish) per §6, gated on the owner decisions in §8
2026-09-05 | Wrappers resolve identity from injected env, then the engine's catalog, never profile variables; catalog carries `url`; `enabled:` rejected on system/admin/broker-with-dependents | Code review on the tenant-ready registry found a rollout hazard in the prior 2026-09-05 identity-injection pass below: `run-pocket-id.sh`/`run-tinyauth.sh` exited 78 the instant `HOME_STACK_SELF_URL` was unset, but that variable's only carrier is a plist the NEW engine generates — `hs restart` is `launchctl kickstart -k`, which re-execs a loaded job under its EXISTING environment (launchd only re-reads a plist on bootstrap), and a reboot loads whatever plist is already installed. Restarting Pocket ID/tinyauth, or rebooting, before `install-launchd.sh --load` reinstalls the new plists would crash-loop both under KeepAlive. Separately, `home_stack_service_url` still recomposed Subdomain/Host/wildcard itself in Perl instead of reading the engine's own computed answer, and `hs app add` fell through `hs`'s unknown-first-level-command case into the `<service> <command>` fallback, handing `service-launchd.sh` action=add target=app instead of a usage error | Keep the immediate exit 78 (correct once every plist is reinstalled, but crash-loops until then); have wrappers recompose the URL from profile variables again as a fallback (reintroduces the exact duplication the identity-injection pass just deleted); leave `hs app add` as-is since `service-launchd.sh` already rejects the bogus action with exit 2 | Each wrapper (pocket-id, tinyauth, hermes) now resolves its own URL as injected `HOME_STACK_SELF_URL` if set, else `home_stack_service_url <its own name>`, else fails closed naming both sources; same fallback shape for `HOME_STACK_DATA_DIR`. `home_stack_service_url` now only reads catalog.json's engine-written `url` key (present only for a routable, enabled, non-wildcard service) and returns 1 otherwise — the shell no longer recomposes a hostname at all. The Go review pass (commit `d220799`) added the matching validation: `enabled: false` is rejected outright on a `type: system` service, on `admin`, and on the auth broker while any enabled service still requires it, naming the dependents; a disabled-but-not-yet-pruned service stays visible to Admin with an info incident and a `stop` action instead of going dark. `hs`'s dispatch now refuses an unrecognized first-level command with usage and exit 2 before the `<service> <command>` fallback is attempted, so only a known lifecycle verb in the second argument position reaches `service-launchd.sh` | OpenTeam | Implemented | `hs stop <svc>` followed by a separate `hs stop <svc>; hs start <svc>` is NOT race-safe: `do_stop` in `service-launchd.sh` fires `launchctl bootout` and returns without waiting for teardown (`home_stack_wait_label_gone` is only used by `do_restart`'s not-loaded fallback and by `install-launchd.sh --load`), so a `start` issued immediately after can race launchd's asynchronous bootout and fail with "Bootstrap failed: 5". Prefer `hs restart <svc>` (kickstart -k, no bootout) or `install-launchd.sh --load` (which waits) when a plist changed
2026-09-05 | Engine-injected per-service identity; no wrapper derives hostnames from profile variables | §3.2 (finding #13): `run-pocket-id.sh`, `run-tinyauth.sh`, and `run-hermes.sh` each recomposed a sibling's or their own public URL from a profile subdomain variable (`HOME_STACK_POCKET_ID_SUBDOMAIN`, `HOME_STACK_TINYAUTH_SUBDOMAIN`) with `:-id`/`:-auth` fallbacks — change one copy and the other silently disagrees, exactly the substitution the 2026-05-02 decision banned. §3.2 also closed SERVICE_INTERFACE §5's `HOME_STACK_DATA_DIR` gap | Keep the profile-variable duplication and just delete the fallbacks (still two sources of truth); inject the FQDN only, leaving wrappers to prepend `https://` themselves; have wrappers shell out to the admin binary to resolve a sibling's URL | `GenerateLaunchdPlist` now injects `HOME_STACK_SELF_NAME`, `HOME_STACK_SELF_HOST`, `HOME_STACK_SELF_URL`, and `HOME_STACK_DATA_DIR` into every managed service's plist, computed from that service's own registry entry — the same composition the Caddyfile generator uses — and un-overridable from the registry (`Validate` already rejects `HOME_STACK_*` in a service's `env:`). A wrapper resolving a *sibling's* URL reads `catalog.json` (already generated, private) via a new `home_stack_service_url` helper in `lib/common.sh`, using core `perl -MJSON::PP` rather than adding a dependency. All three wrappers fail closed (exit 78, naming the missing variable) instead of falling back to a guessed subdomain. Deleted the now-fully-dead `HOME_STACK_ENABLE_*`, `HOME_STACK_POCKET_ID_SUBDOMAIN`/`HOME_STACK_TINYAUTH_SUBDOMAIN`, `HOME_STACK_PHASE_A/B_CADDY_PORT`, and `HOME_STACK_DEV_DOMAIN`, and the `hs app` stub dispatch | OpenTeam | Implemented | None
2026-09-05 | `enabled:` as the registry-only toggle; Portal projection keeps the `lifecycle` enum, uses `launchable: false` | §3.2 called for a registry-driven replacement for the inert `HOME_STACK_ENABLE_*` env flags, plus two latent bugs found on the same generation path: `syncSystem`/`Apply` never deleted a stale plist for a removed or now-disabled service, and `diffPlists` compared plist names only, so a content-only change was invisible to `hs deploy --preview` | Add a `disabled` value to the Portal `lifecycle` enum (a schema version bump, and a cross-repo contract change for a same-process concern); gate on env flags instead of the registry; leave the two plist bugs for a separate PR | `enabled: true\|false` (default `true`) keeps validation, inventory, and the catalog entry intact for a disabled service so it can be safely re-enabled; only Caddy routes and launchd plists disappear, behind one new `Service.generatesAgentPlist()` helper replacing three duplicated type checks. The Portal projection needed no schema change: `url: null` plus `launchable: false` already express "listed, not launchable" without touching the `lifecycle` enum or bumping the schema version. Both bugs sit on the same generation path this touched, so a stale plist is now pruned by name-set diff and `diffPlists` now compares plist content, not just presence | OpenTeam | Implemented | None
2026-09-05 | Direction: reliable on a laptop and migratable to the next Mac outrank features; personal-project sizing — prefix-isolated host tests instead of a VM tier, an inventory table and one command instead of migration tooling, a shared notifier only when a second client exists; tenant apps onboard themselves; Phase D parked; Admin frozen | Owner confirmed the host stays a laptop for the foreseeable future and may move to a new Mac (laptop or desktop) at any time. The 2026-09-04 sequence put remote access, dev gateway, upgrade tooling and Admin polish ahead of a reliability tier that does not exist (`make test-mac` disabled; three tests write into the source tree; a lifecycle bug list unverified; MIGRATION.md omits every path outside git) | Keep the feature-first order; move to a Mac mini now; re-enable the Tart VM tier (three blockers, an image to maintain); an `hs migrate` tool family and an `install:` registry manifest; a notification-hub service now; carry the first tenant app's onboarding on this roadmap | The identifier-prefix parameterization already gives launchd tests a collision-free lane on the host — the same safety as a VM with none of its cost. Migration's hard part is the inventory, not tooling. A shared notifier generalizes one tenant's code and should wait for the second client. Onboarding is the tenant's deliverable; home-stack's is a real contract (P2). Phase D has no customer; its spec is complete and waits. The Admin webapp works and `hs` plus Hermes are the operator surface | OpenTeam | Decided | Re-run the `docs/PLAN.md` audit when P3 lands; revisit the substrate question (Mac mini) if the laptop's sleep/reboot limits bite after P3
2026-09-04 | Plan realignment: registry-only toggles, `remote:` as a closed enum, tenant apps as a named contract, Phase D revalidated against the browser-only off-tailnet requirement | A read-only audit (`docs/PLAN.md`) found ROADMAP presenting shipped work as upcoming, README contradicting STATUS, three docs disagreeing on Admin Basic Auth, inert `HOME_STACK_ENABLE_*` flags, duplicate hostname variables, `make test` overwriting the production binary, and no registry contract for remote access. The first tenant app's design moved to its own repository | Keep env feature flags and make the engine honour them; reuse `auth:` for the remote lane; federate Cloudflare Access to Pocket ID; embed the Access JWT verifier in Admin; ngrok or Tailscale Funnel as the remote perimeter | Registry membership plus a planned `enabled:` field is the only toggle (flags shadowed the registry and gated nothing). The remote lane gets its own `remote: none\|access` field because it is a different gate on a different listener, denied by kind for control/agent/auth/backend/ingress. Access uses OTP plus a hosted IdP so no new login surface is exposed on the Mac; Pocket ID federation is deferred. The verifier is a standalone loopback service so Admin never sits in a public request path. ngrok loses on cost, a second vendor, and corporate-filter blocking; Funnel has no edge auth. Basic Auth stays: it is the repair path for a broken identity layer | OpenTeam | Decided (docs); P2/P3 implement | Supersedes: ROADMAP retire-Basic-Auth bullet; `hs app` family; env feature flags; `remote_access: true` naming. Open: D.2 Pocket ID federation; first `remote: access` customer
2026-08-12 | Let official Hermes own its internal gateway LaunchAgent while Home Stack owns the exposed dashboard | Gateway adoption raised a supervision choice. Upstream generates a profile-aware `ai.hermes.gateway` plist with PATH/runtime refresh, graceful drain, restart throttling, resource limits, code-skew handling, and in-chat restart semantics; the worker has no Home Stack route or listener by default | Add a managed `hermes-gateway` registry entry and wrapper; copy the upstream plist into Home Stack; run both supervisors; leave the gateway foreground-only | A generic Home Stack plist would duplicate and drift from application lifecycle logic while risking two dispatchers against one Hermes state. Keep the dashboard in the registry because Home Stack owns its Tailnet bind/Caddy route. Let Hermes generate and update the internal gateway service, document its label/env/log contract, verify it alongside the stack, and require a new review for any gateway-created listener or public ingress | OpenTeam | Implemented and live-verified with one PID, zero listeners/platforms/jobs, and dashboard status | Configure one sender-allowlisted platform interactively if desired; re-review any listener or public ingress
2026-08-12 | Give tool-capable agents a clean child environment and isolate authority levels operationally | The Hermes wrapper sourced the shared secret layer with export-all semantics and then execed an agent capable of shell/tool execution, giving it unrelated Admin, Cloudflare, identity, and service credentials. The machine dashboard can also select among ordinary Hermes profiles, so profile names alone are not remote authorization boundaries | Keep ambient inheritance; split every home-stack secret file immediately; rely on prompts/approvals; expose privileged profiles through the same machine dashboard | Keep the established loader for profile resolution, then exec Hermes with only user runtime plus Hermes dashboard/OIDC inputs and enforce the absence of stack variables in a negative test. Models, tools, memory, and provider credentials remain Hermes-owned. When broader authority is added, use isolated Hermes homes/processes or narrow reviewed recovery commands: personal/remote operator may be Tailnet-reachable, coding gets explicit worktrees, broad operator remains local/short-lived, and gateway/cron/webhook lanes stay less privileged | OpenTeam | Environment isolation deployed and verified; operating model active | Add later capabilities behind their acceptance gates
2026-08-08 | Retire the bootout/bootstrap pattern for launchd reloads | `launchctl bootout` is asynchronous; bootstrapping the same label before teardown completes fails with "Bootstrap failed: 5: Input/output error" after the job is already dead — service down, nothing restarted it. This bit the identity-layer cutover live, and install-launchd/service-launchd carried the same pattern (restart used a fixed `sleep 1` as its only defence) | Longer fixed sleeps; retry loops around bootstrap; leave as-is | Three rules, shared via `home_stack_wait_label_gone` in common.sh: (1) a loaded AGENT whose plist is unchanged is restarted with `kickstart -k`, which never unloads the label so the race cannot exist -- the Caddy daemon deliberately keeps its original unconditional bootout/bootstrap sequence, because routing it through the same branch regressed the launchd e2e suite on CI for reasons never pinned down, and the race is fixed there by the wait alone; (2) a plist change genuinely requires bootout — poll until launchd forgets the label before bootstrapping; (3) install --load converges instead of restarting the world: it still bootstraps anything not loaded, so --load keeps its contract of leaving every service running, but a loaded service whose plist is byte-identical is skipped rather than bounced -- which removes most bootouts entirely, at the cost of needing an explicit restart to pick up a rebuilt binary behind an unchanged plist. `tests/launchd-race-guard.test.sh` pins the helper's semantics and greps the racy pattern out of both scripts | OpenTeam | Implemented | service-launchd.sh now loads its own env (previously worked only via the hs wrapper); ephemeral-host e2e coverage lands via test-ci-mac
2026-08-08 | home-stack does not model an app's internal configuration | Pocket ID's SMTP settings were briefly driven from the profile so email would be reproducible from config like everything else in this stack | Keep SMTP declarative in the profile; lift every Pocket ID setting into env vars; split some settings between config and UI | Pocket ID only reads settings from the environment when `UI_CONFIG_DISABLED` is set, and that switch is all-or-nothing: declaring SMTP made app name, accent colour, session duration, signup policy and LDAP env-only too, leaving its admin UI read-only for settings that have nothing to do with this stack. The cost landed on the operator daily; the benefit was reproducing one set-once form. home-stack keeps routing, supervision, ports, binds and secrets, and leaves app-internal preferences to the app — the same boundary already stated in ARCHITECTURE.md | OpenTeam | Implemented | The bind fix stays: `ACTORS_HOST` is exposure, not preference, so it remains owned here
2026-08-08 | Tailnet-first identity: `tailscale_auth` at Caddy, tinyauth+Pocket ID behind it | Stack had no identity layer: Admin used Basic Auth, Hermes its own scrypt password, OpenChamber its own passkeys, and nothing was shared. Wanted the most seamless auth possible, ideally passkeys or tailnet-native | Authelia or Authentik (no macOS binaries; Authentik needs ~500MB + Postgres); Kanidm (no release binaries); oauth2-proxy alone (no federation, no zero-click); tinyauth alone (no passkeys of its own) | Three tiers, because seamlessness and centralization are different axes. `tailscale_auth` resolves the connecting peer via local tailscaled WhoIs for genuinely zero-click access — no extra process, no login screen. tinyauth brokers everything else and federates Tailscale + OAuth/OIDC + LDAP later; Pocket ID supplies the passkey ceremony. Apps with non-browser clients (Hermes) integrate directly, since a proxy gate only ever covers the browser route | OpenTeam | Implemented (staged) | Root-side WhoIs unverified; if it fails, emit `forward_auth` to a loopback WhoIs shim run as a LaunchAgent — same registry field, same UX
2026-08-08 | `auth:` registry field as a closed enum, not a directive surface | Per-service opt-in was required so any new service can plug into auth, but the registry must never become a place to write arbitrary Caddy config | Free-form `caddy_directives:` block; a separate auth config file; hardcoding gated hostnames in the engine | Follows the `proxy_identity: upstream` precedent exactly: `none|tailnet|sso|tailnet-or-sso`, each emitting one fixed reviewed block. Validation rejects unknown values, rejects auth on services with no subdomain/host (Caddy cannot gate a route it does not serve, and implying otherwise is worse than failing), and requires a registered broker with an upstream for the sso modes | OpenTeam | Implemented | Absent `auth:` proven byte-identical to prior output; acme/beta goldens unchanged
2026-08-08 | Build for the host's real architecture, not the toolchain's | The Go toolchain and Homebrew are both Intel builds under Rosetta on an M1, so every home-stack binary — including the live Caddy ingress and admin service — was x86_64 and running translated | Install a second arm64 Homebrew; install a native Go alongside; accept the Rosetta tax | Cross-compilation needs no new toolchain: the Makefile derives GOARCH from `uname -m`, so Linux CI is unaffected. CGO_ENABLED=1 and CC are set explicitly on Darwin because Go silently disables cgo when GOARCH differs from the toolchain's, which would switch the admin to Go's pure resolver and break MagicDNS health checks | OpenTeam | Implemented | Existing binaries staged, not swapped; cutover is operator-run
2026-08-08 | Build tinyauth from source; keep containers designed but unadopted | tinyauth publishes Linux-only binaries, and it is the one component in the identity layer without a macOS release | Run it under Docker/OrbStack; adopt Apple `container` (available: M1 + macOS 26); pick oauth2-proxy instead (official darwin binary, but no federation and no tailnet auto-auth) | It is an ordinary Go program with a pnpm-built embedded frontend and pure-Go SQLite, so a source build costs one pinned recipe — the same arrangement `caddy-cloudflare` already has. Containers were designed (`type: container`, one per service, Apple `container` over Docker) but not adopted: launchd has no dependency graph, so a container runtime becomes a second login-gated prerequisite beneath every service, deepening the FileVault stall already documented in the README. Auth infrastructure is the worst place to add boot-order risk | OpenTeam | Implemented | Pin the tinyauth commit on upgrade; revisit containers only when a service genuinely cannot build natively
2026-08-08 | Aligned every Caddy build recipe on one plugin set | bootstrap.sh, the Tart image, and the Linux test image each built Caddy with a different plugin set (`caddy-security` appeared in two, absent from the live binary and RUNBOOK), and fixtures now emit `tailscale_auth` | Leave recipes alone and document the drift; make the tailscale plugin optional in generation | A test image lacking the plugin fails on valid config, so drift becomes a false CI failure. All recipes now build cloudflare + tailscale for the native arch; the vestigial caddy-security is dropped. Also fixed `profile-portability.test.sh`, which hard-failed `caddy validate` against any stock `caddy` on PATH — and could never have passed anywhere, since the cloudflare provider rejects placeholder tokens at provision time. It now prefers a plugin-capable binary and uses `adapt` | OpenTeam | Implemented | Caddy upgrades must preserve BOTH plugins; the binary is now load-bearing for auth as well as TLS
2026-08-08 | Publish sanitized Portal projections through the Portal origin | Portal v2 must discover every registered service and show honest health without consuming the infrastructure-shaped catalog or authenticated Admin API | Expose raw `catalog.json`; let Portal call Admin with credentials; maintain a second Portal registry; publish sample data | Home Stack remains the sole authority while two strict versioned projections expose only launch/navigation metadata and coarse health. Caddy proxies two exact same-origin paths, handlers require the Portal Host, and Admin credentials/mutations/details/PIDs/paths/upstreams remain excluded | OpenTeam | Implemented | Canonical schemas live in Home Stack and are exercised by projection tests; version them deliberately and keep unknown registry services visible by default
2026-08-07 | Allow Hermes as a narrowly scoped authenticated native-protocol service | Current official Hermes enables its fail-closed auth gate only for non-loopback binds, and compatible remote clients/WebSockets cannot operate through a loopback listener; a Tailnet-IP bind conflicts with the ordinary Caddy-only ingress rule | Loopback behind Caddy (no native auth/remote WS); bind `0.0.0.0` (excess exposure); add loopback aliases/pf/second proxy; skip compatible native clients | Bind only `HOME_STACK_TAILNET_IP`, require the native `basic` provider with scrypt hash and stable signing secret, retain Caddy HTTPS with fixed upstream Host/Origin identity, expose no LAN/public/Tunnel route, and verify `/api/status` before declaring safe | OpenTeam | Implemented and production-verified | Keep upgrades commit-pinned and repeat listener/auth/HTTPS/WebSocket/Admin/restart checks after every change
2026-05-03 | Delete templates/Caddyfile.template — engine-only Caddyfile generation | `render-caddyfile.sh` was overwriting the registry-generated Caddyfile with a hardcoded template, making engine generation dead code and producing Caddyfiles that did not reflect `services.yaml` | Keep both paths with merging; make template the fallback | The engine already generates a correct registry-driven Caddyfile. Having a template overwrite it was a source of drift (the template had hardcoded portal/opencode entries not in the registry). The template is deleted; `render-caddyfile.sh` remains for local dev modes only | OpenTeam | Implemented | Remove Caddyfile.template references from ROADMAP 
2026-05-03 | Relax validateCaddyToken to validateToken for service fields | Service `args`, `binary`, `working_dir`, and `env` values were validated by `validateCaddyToken` which rejects whitespace. This broke the `test-app` service whose bash args contain spaces, causing the admin webapp to return 500 errors | Add a per-field flag; remove the whitespace check entirely | Only Caddy-token fields (upstream, email, etc.) need whitespace rejection. Service configuration fields should use `validateToken` which blocks control characters but allows spaces | OpenTeam | Implemented | None
2026-05-03 | reload-caddy.sh no longer calls render-caddyfile.sh | The reload script was rendering the template and overwriting the engine-generated Caddyfile before validating | Keep the render call; make it conditional | The engine writes the Caddyfile during sync. The reload script should validate and reload that file directly without re-rendering | OpenTeam | Implemented | Delete templates/Caddyfile.template
2026-05-03 | Test suite expansion complete | 19 tests across three tiers: host (<10s), Linux container (<30s), Tart macOS VM (<90s) | GitHub Actions CI; fewer manual tests | Local containerized + VM tests give fast feedback without CI cost for a one-person repo | OpenTeam | Implemented | Keep test files current as codebase evolves
2026-04-26 | Namespace: a personal domain under the owner's control | Initial plan namespace | Nexus, mesh, studio, or another generic subdomain name | Selected for clarity and durability | OpenTeam | Proposed | None yet
2026-04-26 | Go-based Caddy Build | Brew tap failed due to Git auth | curl download, submodule | Go build ensures official xcaddy plugins while bypassing Git HTTPS issues | OpenTeam | Implemented | Add to bootstrap script
2026-04-26 | No Binary Vendoring | Keep repo light | Commit binary to repo | Binaries are ~40MB and arch-specific; robust build script is better for portability | OpenTeam | Decided | None
2026-04-26 | Static Portal | Decide portal tech | Node app, external service | Serve static files from Caddy to minimize overhead and maximize reliability | OpenTeam | Decided | Create portal-www dir
2026-04-26 | Dynamic Caddyfile Rendering | Scale to many subdomains | Manual Caddyfile edits | Future enhancement: render-caddyfile.sh should iterate over profile-backed services.yaml after Phase E | OpenTeam | Decided | Not yet implemented
2026-04-27 | Dual-Perimeter Architecture (Phase D) | Need secure remote access off-Tailnet | VPN-only, public Caddy with basic auth | Combine Cloudflare Tunnels (Edge Gating/Identity) with local Caddy (Host Verification) | OpenTeam | Planned Extension | None yet
2026-04-28 | Profiles And Config Boundary (Phase E) | Need portability for the owner's machines and optional reuse by others | Keep all config in scripts, track ~/.config directly | Tracked non-secret profiles plus untracked local overrides/secrets keep desired state portable without committing runtime state | OpenTeam | Planned Extension | Add `profiles/<name>`, config loading order, and feature flags
2026-04-28 | App Deployment Layer (Phase F) | Need fast local deployment for temporary and durable internal apps | Manual Caddy edits, separate hosting per app | Registry plus helpers can expose static, proxy, split, and container-backed apps from any project directory while keeping home-stack as ingress only | OpenTeam | Planned Extension | Start with static-only helper flow after Phase E
2026-04-28 | Dev Gateway (Phase G) | Need stable Tailnet URLs and catalog for local dev servers without per-server Caddy edits | Path-only proxy under portal, manual subdomains | A fixed `dev.<parent-domain>` catalog plus `*.dev.<parent-domain>` host-based gateway avoids path-prefix breakage and keeps Caddy config static | OpenTeam | Planned Extension | Requires TLS/DNS for `dev.<parent-domain>` and `*.dev.<parent-domain>`
2026-04-28 | Rich Portal/Admin Boundary (Phase H) | Need independent rich portal work without coupling UI to infra repo | Keep portal app inside home-stack, make portal proxy apps directly | Separate static Portal repo consumes Home Stack-owned sanitized catalog/status projections; Admin remains a separate authenticated control plane | OpenTeam | Implemented | Portal v2 is deployed as a static PWA; exact same-origin projection routes and versioned schemas preserve the boundary
2026-04-30 | UI Design: OLED Stealth 2.0 & Aero Core | Need distinct identities for control (Admin) and discovery (Portal) | Single unified theme | Admin explored high-density 'Ghost in the Machine' aesthetics and Portal explored 'Personal OS' aesthetics | Creative Expert | Superseded | Superseded by mobile-first Admin control-plane decision below; Portal remains separate from Admin
2026-04-30 | Admin as Mobile-First Control Plane | Admin v1 needed to be useful on iPhone while remaining safe for Caddy/launchd operations | Keep dense micro-dashboard, merge admin with portal, expose direct per-route mutations | Admin is a separate Tailnet-only control plane with triage, services, deploy preview, events, doctor checks, unified typed actions, event audit, and iPhone-first service sheets | OpenTeam | Implemented | Continue refining mobile UX; add richer deploy/app workflows and server-provided action descriptors
2026-04-30 | Canonical Status And Roadmap Docs | Plan checkboxes and implementation drift made it unclear what was current | Keep updating every historical plan, rely on one root plan | `docs/STATUS.md` tracks implementation truth and `docs/ROADMAP.md` tracks current planned work; historical plans remain preserved but non-canonical | OpenTeam | Implemented | Keep status/roadmap updated after each implementation session
2026-05-02 | No Owner-Specific Fallbacks | Generator code and scripts silently substituted the maintainer's own domain, identifier prefix, ACME email, and Tailnet IP when profile vars were absent, masking profile bugs and blocking other machines | Mark fallbacks as samples; keep current behavior with warnings; gate behind a "strict" flag | Profile portability requires that missing mandatory configuration produce a clear error at the right layer; silent substitution made profile correctness unobservable and let owner-specific values leak into generated artifacts | OpenTeam | Decided | Implement in Phase 1 minimum slice (engine.go, common.sh, launchd scripts); see ROADMAP Phase 1 Design Notes
2026-05-02 | Configuration Loading Layers | Needed clear precedence among tracked profile, machine-local override, and secrets | Single profile + secrets only; profile + arbitrary machine env | Profile (tracked) → `config.env` (untracked machine override) → `env.local` (untracked secrets) gives a single non-secret override hatch without polluting the secret file or the tracked profile; identity keys must not appear in `env.local` | OpenTeam | Decided | Wire `config.env` loading into `scripts/lib/common.sh` after profile sourcing; reject identity keys in `env.local`
2026-05-02 | Generated Artifacts Gitignored | `Caddyfile`, `catalog.json`, and `launchd/*.plist` were committed with owner-specific values | Keep checked in and regenerate over them; move to a runtime dir outside the repo | The engine already regenerates these on sync/install/startup; checked-in copies are state, not source; gitignoring them removes a profile-leak vector and lets `hs sync` start from a clean tree | OpenTeam | Decided | Add `.gitignore` entries; delete current checked-in copies in Phase 1 step 8
2026-05-02 | Registry Stays Pure Data | `services.yaml` needed a way to express profile-aware hosts | Allow shell-style `${VAR}` interpolation in YAML; require absolute FQDNs only | Pure data with engine-side composition keeps the registry portable across profiles, avoids YAML/shell coupling, and lets validation reject ambiguous configs; `subdomain`/`host` fields cover wildcards and external hosts cleanly | OpenTeam | Decided | Update `services.yaml` schema and engine renderer in Phase 1 step 6
2026-05-02 | CLI Sync/App Add Disposition | `hs sync` and `hs app add` call unregistered Admin endpoints | Rewire through `/api/actions` immediately; hide behind a dev flag | Removing the broken HTTP wiring and reintroducing `hs sync` as a local engine invocation unblocks profile work without holding it on Priority 4 (deploy preview/apply); `hs app add` is correctly deferred to Phase F under the typed action model | OpenTeam | Decided | Delete broken handlers in Phase 1 step 9; ship local-invocation `hs sync`; reintroduce remote sync via `/api/actions` later
2026-05-04 | Registry management via shared Go logic | CLI and webapp needed a way to add/remove services from the registry without manual YAML editing | Separate CLI-only and web-only paths; gum-only interactive flow | Shared Go `registry.go` with `AddService`, `RemoveService`, `Diff`, `Apply` called by both `hs` CLI and typed `/api/actions`. Atomic writes (tmp → validate → rename). CLI uses gum for interactive prompts; webapp uses sheet component with type/lifecycle visual cards. | OpenTeam | Implemented | Add service edit/remove from webapp detail sheet
2026-05-04 | `type: split` service shape | Needed to support SPAs where Caddy serves frontend assets AND proxies API calls | Add two separate services (one static, one proxy); use path-based routing | Single `type: split` with `root` (static assets), `upstream` (API backend), and `api_path` (proxy prefix, default /api). Generates Caddy config with nested handle blocks. Simpler than two separate entries. | OpenTeam | Implemented | Support comma-separated api_path for multiple prefixes
2026-05-04 | Service lifecycle field (managed/external/custom) | Services need different levels of home-stack integration | Force all services to be managed; use type field to determine management | `lifecycle` field: `managed` (generates plist from binary/args), `external` (only Caddy route), `custom` (install existing plist). Defaults to managed when binary present, external otherwise. Keeps registry declarative. | OpenTeam | Implemented | None
2026-05-04 | Admin binary persistent location | `/tmp/` is cleared on reboot, breaking auto-start | Keep /tmp/ with rebuild; move to ~/bin/ | Binary at `portable/home-stack/admin/home-stack-admin` (gitignored). `run-admin.sh` only rebuilds if missing. Survives reboots. | OpenTeam | Implemented | None
